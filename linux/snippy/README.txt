Snippy is a bash script that copies some text, and replaces that text with the text in a file with the same name and the .snip extension.

To use this, put the script somewhere (e.g. ~/.snip) and setup a shortcut key combination (e.g. Ctrl+Alt+UpArrow).

